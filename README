# 🌤️ Extraindo Dados Meteorológicos com FastAPI

Este projeto tem como objetivo ***coletar, armazenar e disponibilizar dados meteorológicos*** de diferentes cidades utilizando **Python, FastAPI e SQLite**.  
A aplicação consome dados de uma API externa, salva as informações localmente em um banco de dados e disponibiliza endpoints REST para consulta e gerenciamento.

---

## ⚙️ Tecnologias Utilizadas

- Python 3.10+
- FastAPI
- Uvicorn
- Requests
- PostgreSQL
- psycopg2
- dotenv

---

## 📂 Estrutura do Projeto

📁 extraindo-dados/
│
├── app/
│ ├── api.py # Endpoints da aplicação (FastAPI)
│ ├── database.py # Conexão e operações no PostgreSQL
│ ├── dataWeather.py # Integração com API externa de clima
│ ├── main.py # Script inicial para criar a tabela e popular o banco (dispensável)
│
├── .env # Variáveis de ambiente (API keys, configs)
├── requirements.txt # Dependências do projeto
└── README.md # Documentação do projeto


---

## 🧠 Instalação e Configuração

1. **Clone o repositório:**
   git clone https://github.com/seu-usuario/extraindo-dados.git
   cd extraindo-dados

2. **Crie e ative o ambiente virtual:**

python -m venv venv # (Cria o ambiente)
venv\Scripts\activate   # (Windows)
source venv/bin/activate  # (Linux/Mac)

3. **Instale as dependências:**

pip install -r requirements.txt

4. **Crie o arquivo .env com suas credenciais PostgreSQL:**

DB_HOST=localhost
DB_PORT=5432
DB_NAME=fastweather #(Exemplo)
DB_USER=seu_usuario
DB_PASSWORD=sua_senha


5. **Execute a aplicação:**

``uvicorn app.api:app --reload``

6. **Acesse a documentação interativa:**

Obs: verifique no seu terminal a porta que sua api está sendo rodada, a minha está rodando na 8000
Ex:
Swagger UI → http://127.0.0.1:8000/docs

**🧠 Funcionalidades**

✅ Buscar dados meteorológicos de uma cidade específica
✅ Salvar os dados no banco de dados PostgreSQL
✅ Consultar todos os registros armazenados
✅ API documentada automaticamente via Swagger

**📡 Endpoints Principais**

| Método  | Endpoint       | Descrição                                                  |
| :------ | :------------- | :--------------------------------------------------------- |
| **GET** | `/weather`     | Consulta dados climáticos de uma cidade (e salva no banco) |
| **GET** | `/weather/all` | Retorna todos os registros salvos no PostgreSQL            |



**💾 Exemplo de Resposta /weather?city=Florianopolis**

{
  "message": "Dados de Florianopolis salvos com sucesso!"
}

**🧱 Banco de Dados (PostgreSQL)**
A tabela weather é criada automaticamente no primeiro uso:

#CREATE TABLE IF NOT EXISTS weather (
#    id SERIAL PRIMARY KEY,
#    city VARCHAR(100),
#    temperature REAL,
#    humidity INTEGER,
#    description VARCHAR(255),
#    date TIMESTAMP
#);
    


**🚀 Próximos Passos (Melhorias Futuras)**

 - Adicionar autenticação por token
 - Implementar filtros por cidade e data
 - Criar dashboard com visualização dos dados
 - Containerizar com Docker.

👨‍💻 Autor

John Helder
Desenvolvedor promissor que curte dados e soluções inteligentes.
📧 johnheldercs@gmail.com

💼 LinkedIn: https://www.linkedin.com/in/john-helder


📝 Licença

Este projeto está sob a licença MIT.
Sinta-se à vontade para usar, modificar e compartilhar. 😊