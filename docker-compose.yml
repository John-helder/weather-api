version: '3.8'

services:
  web:
    build: .
    container_name: weather_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: uvicorn app.api:app --host 0.0.0.0 --port 8000 --reload

